# 交接文件 2026-09-21 — 3D 精裝書契約已寫好，待派工

> 更新日期：2026-09-21
> **本檔只記錄「狀態、歷史、待辦」。規則在 `AGENTS.md`（實作）與 `spec.md` §11（規格，本輪新增 §11.5）、`CLAUDE.md`（派工脈絡）。**
> 上一份是 `HANDOFF-2026-09-16.md`，保留為歷史快照。本檔 §1 以後沿用自該檔，§0 與 §6 是本輪新增。

---

## 0. 🔜 下一輪起來先做這個

**Frank 指示**：universe_bot 的突變測試跑完、Frank 叫起下一輪之後，才派工（避免兩邊同時吃 CPU）。

1. 開場檢查：`git fetch`＋`git status -sb`＋`git log -1`＋`git worktree list`
   （`oldrepo` 是 prunable 的舊 worktree，不是本輪產物，要不要 prune 問 Frank）。
2. 確認契約還是紅的：`node tools/check_book3d.mjs`（本輪結束時 59 FAIL／51 PASS）、`node tools/check_card_contrast.mjs`（4 FAIL）。
3. **派工**：工作包 `.git/wp/P-book3d.md`（共同條款 `.git/wp/COMMON.md`）。
   **Frank 決定用 Claude agent，不用免費席次**（見 §6.3）。建議 Sonnet＋`isolation: worktree`，可動檔案只有 `assets/css/style.css`。
4. Review：讀 diff、跑 §6.4 列出的八組測試、**自己截圖目視**（1280／360，首頁＋書庫＋詳情頁）。
   可以跟參考實作的截圖比對：`.git/wp/shots/book3d-ref-*.png`。
5. `/security-review`：只改 CSS、沒有輸入面，照 `CLAUDE.md` §5「動到程式碼就要重跑」的原則還是跑一次，成本很低。
6. push 等 Frank 逐次授權。本地目前比 origin 多 2 個交接 commit ＋本輪的 commit（見 §6.5）。

## 0.1 目前進度（沿用 09-16，未變動的部分）

| 項目 | 狀態 |
|---|---|
| 書池 `manga.json` | 39 本，**新增必填 `ended` 欄位**：完結 30、連載中 9 |
| 必讀經典 | 固定收藏 19 本 ＋ 書池完結 30 本 ＝ **49 本** |
| 有生之年 | 18 本，14 本規格外標籤已歸位（`genre` 全站只剩 15 個規定詞彙）|
| 書庫 `library.html` | **新頁**，三檔合併 76 本，搜尋／類型／完結篩選，`?q=` 同步網址 |
| 視覺 | 胡桃木書房配色、固定深色、系統楷體標題、正面書卡 |
| Push | 兩輪都已上線：第一輪 `133cce4`（02:03）、第二輪 `366f75f`（02:17）|
| 🔴 **待修** | 首頁第二排空白：**已確認是瀏覽器快取新舊檔混用**（強制重整即恢復），根治排入下一輪（見 §5）|

### 更正上一份交接文件

`HANDOFF-2026-09-12.md` §0／§5 寫「第六批尚未 push」，**實際上 `97a967f` 當時已在 `origin/master`**（2026-09-16 以 `git branch -r --contains` 查證）。
那份是歷史快照不改，在此更正。

---

## 1. 為什麼改版（起因）

2026-09-16 Frank 問「選這些書要用在哪？」。查證結果：首頁只放華文圈前 10 名（`fetch_ranking.py` 的 `TOP_N = 10`），
其餘書沒有任何入口。翻 9/5~9/15 共 12 天的 `ranking.json` 歷史，**39 本裡有 20 本從未上榜**，第六批 7 本只有地獄樂上過 1 天。
⇒ Frank 裁示：完結的書全部進必讀經典（可與熱門榜重複）、加書庫與搜尋、先做 UI 改版。配色從三套提案選 B「胡桃木書房」。

---

## 2. 派工與 review 紀錄（三席 opencode × Muse Spark 1.3 Free）

| 包 | 席位 | 內容 | 結果 |
|---|---|---|---|
| P-data | w3:p4 | `ended`＋有生之年標籤歸位 | 一次過，diff 只有指定欄位 |
| P-lib | w3:pS | `library.js` 純函式 | 一次過，22 項契約全綠 |
| P-ui | w3:pT | 配色、書卡、導覽列 | 測試全綠，**但 review 截圖抓到兩個問題** → P-fix |
| P-fix | w3:pT | 書卡對比、首頁留白 | 一次過 |
| P-page2 | w3:pS | 書庫頁、必讀經典合併 | 一次過，並主動回報 3 個它無權修的樣式缺口 → P-lib3 |
| P-lib3 | w3:pT | 書庫收尾（badge、搜尋框、換行、2 欄）| 一次過 |

契約全部是「先寫會紅的測試」：`tools/test_library.mjs`、`test_data_status.py`、`check_ui.mjs`、`check_library_page.mjs`、`check_card_contrast.mjs`
（加上既有 `check_pages.mjs`，共 6 組，**2026-09-16 全綠**）。

### 2.1 本輪教訓

1. **測試全綠 ≠ 畫面能看。** P-ui 測試全過，但真 Chrome 裡淺色書卡白底白字。
   用全部 76 本的真實 `color` 算 WCAG 對比：**50 本 < 3.0**。已寫成 `check_card_contrast.mjs`（讀 CSS 混色比例計算），以後改配色不會退步。
2. **headless 截圖會拍在 CSS 進場動畫中途**，兩次讓我誤判「書架只剩 3 本」。反過來，真 Chrome 擴充截圖的顏色會失真偏淡——
   **顏色以 `getComputedStyle` 讀值為準、互動以頁內 JS 觸發後讀 DOM 為準**，不要只看圖。
3. **我寫的契約測試有一個 bug**：`test_data_status.py` 原本以 `HEAD` 當比對基準，資料 commit 進去後基準跟著移動、整批誤報。已改為固定 `84c342e`。
4. **opencode 存截圖到 `/tmp` 會跳「存取外部資料夾」權限提示**，builder 就卡在 blocked。之後工作包一律指定存 `.git/wp/shots/`。
5. **免費席次在「紅測試契約＋明確可動檔案清單」下表現穩定**：三席平行零互撞、零改測試、零假資料 fallback；
   P-page2 遇到自己無權改的 `style.css` 缺口時照實回報，沒有越界。

---

## 3. 🔜 待辦

### 3.1 需要 Frank 決定

- `README.md` 第 1 行仍是「台灣前 10 大熱門漫畫榜」，與現況不符，要不要改是文案決定，本輪未動。

### 3.2 已知小尾巴（不影響功能，本輪只提不改）

- `app.js` 有兩處「`library.js` 沒載入就退回舊寫法」的防禦分支（必讀經典 badge、書庫 badge）。正常情況跑不到，不會顯示假資料。
- 書庫頁導覽列的站內搜尋框（`#siteSearch`）不會預填目前的 `?q=`；書庫自己的搜尋框（`#libSearch`）有。
- 詳情頁封面仍是舊的 `coverHTML` 立體書（已換新色），與書卡造型不同。

### 3.3 沿用上一份的待辦（未處理）

- 舊書禁用評論詞、6 本章節標題違規基準線（`HANDOFF-2026-09-12.md` §4.1）。
- **新增書時 `ended` 必填**（`AGENTS.md` §4），選書仍照 09-12 §4.3「2010 年代、已完結、話號明確」的方向，且第一步先跨三檔去重。

---

## 3.5 上線後追加（同日第二輪）

v2.0 於 2026-09-16 02:03（台灣時間）上線後，Frank 回報兩點：

1. **首頁只有一排書，配色稿是兩排**：是 review 端工作包漏寫（只寫「保留現有書架」），不是 builder 做錯。
   Frank 選 B：第二排「書庫精選」＝書池中不在當日前 10 名的 10 本，`dailyPicks()` 依 `xfnv1a(id + 資料日期)` 排序，每日換一批、不顯示名次。
   規格 `spec.md` §11.1，契約 `tools/check_home_picks.mjs`。
2. **導覽列少了全球榜**：也是上一輪工作包讓 builder 拿掉的。主對話直接補回六頁的 `index.html#globalList` 連結（Frank 指示「順手補」）。
   補完後手機 360px 導覽列擠成兩行，再縮小窄螢幕間距（`max-width: 479px` 時 `gap: 12px`）。

## 4. Push 狀態

✅ **第一輪 10 個 commit 已於 2026-09-16 02:03（台灣時間）經 Frank 授權 push 並上線**（Pages 建置 `133cce4` 為 built，線上 `library.html` 回 200）：

```
84c342e v2.0 改版契約（spec §11、AGENTS.md、三組測試）
585d025 資料整理（ended、有生之年標籤）
0d050f1 書庫邏輯 library.js
17c49ef 胡桃木改版第一階段
9483c7e 第二階段與修正輪契約測試
3252be9 窄螢幕溢出修正
dcc5d22 書卡白字可讀性與首頁留白
3da5e29 第二階段：書庫頁、必讀經典合併
551010d 書庫頁收尾契約
133cce4 書庫頁收尾
```

改動範圍包含 `assets/js/*.js` 與 HTML，依 `CLAUDE.md` §5 已跑 `/security-review`，結論見下。

### 4.1 `/security-review` 結論

**無高／中風險發現**（2026-09-16，範圍 `origin/master..133cce4`，由 Sonnet 子代理逐一追蹤資料流，Observation 層、未另行複核）。
追過的入口與出口：`?q=`／`?id=`／`?ch=`／`?src=`、`#libSearch`、`#siteSearch` → `cardHTML()`（所有欄位與 href、`--c` 顏色皆經 `esc()`）、
`renderLibrary()`（查詢字串進 innerHTML 時有 `esc()`，進計數用 `textContent`）、`syncURL()`（固定路徑＋`encodeURIComponent`）、
類型／狀態按鈕（`data-g` 經 `esc()`、`data-s` 為固定值）、`bookHref()`（`encodeURIComponent(id)`＋白名單 src）。
查詢字串跳脫另有契約測試守著（`check_library_page.mjs` 以 `?q=<img src=x onerror=alert(1)>` 在 node:vm 驗證，**不是真瀏覽器實測**）；
真 Chrome 只實測了書庫互動（篩選 76→9→7、搜尋→1、網址同步）與 `getComputedStyle` 顏色。
備註（範圍外、不列發現）：`copyBook()` 逐鍵複製若資料檔含 `__proto__` 鍵會改到區域物件原型，但資料檔由 repo 控制，非攻擊者可達。

### 4.2 第二輪（§3.5）

✅ **已於 2026-09-16 02:17（台灣時間）經 Frank 授權 push 並上線**（Pages 建置 `366f75f` 為 built；線上 index.html 含 `id="shelf2"`、library.js 含 `dailyPicks`、導覽列含全球榜）：

```
0668373 交接文件 2026-09-16（本檔初版）
06e6093 首頁兩排書架契約（spec §11.1、check_home_picks.mjs）
374ae37 library.js 新增 dailyPicks()
9bd67b1 首頁兩排書架：第二排書庫精選
558d7aa 導覽列補回全球榜
b6c85d9 手機導覽列縮小間距
＋ 本檔第二輪更新
```

七組契約測試全綠。`/security-review`（範圍 `git diff origin/master`，Sonnet 子代理）：**無發現**——
第二排沿用 `cardHTML()`／`bookHref()`／`esc()`；`dailyPicks` 的輸入只有 repo 資料與資料日期，不經網址參數；全球榜為寫死的站內錨點。
（手機導覽列間距那條 CSS 是審查開始後才 commit，純靜態樣式，無輸入面。）

---

## 5. 🔴 下一輪待修：首頁第二排「書庫精選」空白

### 5.1 Frank 回報（2026-09-16 第二輪上線後）

> 「第二排沒有書在滑動，目前是空白的，這個交給下一輪再修正」

**尚未取得的資訊**：Frank 的瀏覽器種類、視窗寬度、是否硬重整過、主控台有沒有錯誤、第二排是「整排空白」還是「有標題與層板但沒有卡片」。

### 5.1.1 ✅ 原因已確認（同日追加）

**Evidence（Frank 人眼）**：Frank 在首頁按 **Ctrl+Shift+R 強制重新載入後，第二排就出現了**。
⇒ 原因是 §5.2 Inference 第 1 條「**快取時間差**」：瀏覽器沿用了 `max-age=600` 期間內的舊 `app.js`／`library.js`，
配上新的 `index.html`，舊程式不會 render `#shelf2`，新程式的防禦分支又讓缺函式時**安靜跳過**，所以空白且不報錯。
**第 2、3 條（進場動畫、漂移起點）排除，不用改。** §5.2 以下保留為查證過程紀錄。

**下一輪修法（已收斂，仍照 §5.3 第 3 條先寫紅測試再派工）**
1. **所有頁面引用 JS／CSS 時加版本參數**（例如 `assets/js/app.js?v=<commit 短碼或日期>`），讓每次上線都強迫瀏覽器抓新檔，新舊不再混用。
   要決定版本字串由誰更新：手動改六頁容易漏，建議用測試釘住「六頁的版本參數一致」。
2. **移除 `app.js` 的「找不到 library.js 函式就跳過」防禦分支**（`renderIndex` 的 `typeof dailyPicks`、必讀經典與書庫 badge 的 `typeof essentialsList`／`allBooks`、`renderFixed` 的 `typeof bookHref`），
   讓載入失敗時浮出錯誤，而不是無聲地少一排。
3. 驗收：上線後**不強制重整**、直接一般重新整理也要看得到第二排（在第一次上線後的 10 分鐘內測）。

### 5.2 已查到的（依證據強度分層）

**Evidence（可重查）**
- 本機 `node tools/check_home_picks.mjs` 全綠：vm 環境裡 `#shelf2` render 出 10 張卡、順序等於 `dailyPicks`。
- 本機 headless Chrome 截圖（1280 寬）第二排有 6 張以上卡片可見（`.git/wp/shots/picks-index.png`、scratchpad `ui3/index.png`）。
- 線上三個檔 `curl -I` 皆 `cache-control: max-age=600`、`last-modified: 2026-09-15 18:17:35 GMT`（＝台灣 09-16 02:17）。

**Observation（主對話 claude-in-chrome 載入線上首頁，單次、未複核，視窗寬 702px）**
- 所有 JS 為網路載入（非快取），`typeof dailyPicks === "function"`，`#shelf2` 有 20 個 `<li>`（10 本＋10 個循環複本）。
- 但第一張卡的 `.book-card` 計算樣式 `opacity: 0`、`animation-name: shelf-in`、`transform: translateX(80px)`，**7 秒後仍是 0**；兩排 `<ol>` 的 `style.transform` 皆為空字串（漂移未開始）。
- 同一時間的擴充截圖卻看得到第二排有書（截圖會把分頁切到前景）。
  ⚠️ **這組讀數很可能受「分頁在背景」影響**（瀏覽器會暫停背景分頁的 CSS 動畫與 `requestAnimationFrame`），**不能當成已重現**。

**Inference（尚未驗證的可能原因，依可能性排序）**
1. **快取時間差**：`max-age=600` 讓瀏覽器可沿用舊檔 10 分鐘。若 02:03～02:17 之間開過網站，02:17 後可能拿到「新 index.html＋舊 app.js／library.js」。
   舊 app.js 沒有第二排的 render；新 app.js 裡又有 `if (shelf2 && pool && typeof dailyPicks === 'function')` 這種**找不到就安靜跳過**的防禦寫法，
   兩種組合都會讓第二排空白而**不報錯**。→ 若硬重整（Ctrl+Shift+R）後恢復，就是這個。
2. **進場動畫卡在起點**：`style.css` 的 `.shelf li > * { animation: shelf-in .8s both; animation-delay: calc(var(--i) * 60ms) }`，
   `both` 讓卡片在動畫開始前維持 `opacity: 0`。若在某些情況下動畫沒有跑完（例如第二排初次進入視窗時的時序、或 `initShelfMotion` 複製節點與動畫互相干擾），卡片會一直透明。
   第一排卡片同樣受這條規則影響，但 Frank 只回報第二排，所以要比對兩排差在哪。
3. **反向漂移的起始位置**：第二排 `vel = +28`，`x` 由 0 開始會立刻被 `while (x > 0) x -= groupW` 拉到 `-groupW` 附近。若 `groupW` 在量測當下為 0（例如圖層尚未排版），位置可能跑到視窗外。

**Unknown**
- Frank 那邊實際是哪一種空白、在哪個寬度出現、硬重整後是否仍然空白。

### 5.3 下一輪建議步驟（先查證再派工，不要直接改）

1. **先請 Frank 硬重整（Ctrl+Shift+R）再看一次**。恢復 → 原因 1，修法是移除 `app.js` 那兩處「找不到函式就跳過」的防禦分支（讓錯誤浮出來），並考慮在 HTML 引用 JS 時加版本參數避免新舊混用；**不必改動畫**。
2. 仍空白 → 請 Frank 在該頁主控台貼上以下指令，回傳結果：
   ```js
   (() => { const s = document.getElementById('shelf2'); const c = s && s.querySelector('.book-card'); const cs = c && getComputedStyle(c);
     return JSON.stringify({ dailyPicks: typeof dailyPicks, li: s && s.children.length, opacity: cs && cs.opacity, anim: cs && cs.animationName,
       transform: s && s.style.transform, width: innerWidth, errors: '見主控台紅字' }); })()
   ```
   - `li` 為 0 → 資料沒 render（原因 1 或 JS 錯誤）
   - `li` 為 20 但 `opacity` 為 0 → 原因 2（動畫）
   - `opacity` 為 1 但畫面沒卡 → 原因 3（漂移位置）
3. 確認原因後**先補一條會紅的測試再派工**。原因 2、3 屬於真瀏覽器行為，vm 測試抓不到：驗收要用**前景分頁**的 claude-in-chrome 或 headless 截圖，
   並用 `getComputedStyle` 讀卡片透明度與 `<ol>` 位移，不要只看截圖（本輪兩次被截圖誤導，見 §2.1 第 2 條）。
4. 修完順手處理：`app.js` 的防禦分支（§3.2 第 1 條）正是讓這次故障**無聲**的原因之一，建議一起移除。

---

## 6. 2026-09-21：3D 精裝書（契約已寫，尚未派工）

### 6.1 起因與決策過程
Frank 給了 4 張 3D 書本 mockup 圖，問能不能把站上的書都做成這樣。主對話在 scratchpad 做原型給 Frank 看，逐步定案：
1. 純 CSS 3D（`rotateY`＋`::before` 書脊），不需要封面圖。第一版書脊只露一條細線，加大角度到 38 度、透視拉近才像。
2. Frank 指出參考圖的立體感來自「**書脊與封面之間的摺痕＋陰影**」，要求漸層 → 加了摺痕層、地面陰影。
3. 漸層方向：參考圖是上暗下亮，但底部作者／類型是白色小字，下亮會讀不清 → Frank 同意**上亮下暗**。**轉正保留**。
4. Frank 問「要展現陰影，底色是不是要改」→ 對，`#1b1411` 太接近黑，陰影壓不下去。三案比較後 Frank **選 C：書架區換淺一階胡桃木牆**。
   不改 `--ground`（書卡混色以它為準），另開 `--wall-top`／`--wall`。
5. Frank 追加：「**書本顏色不要再用華文圈排行榜那種強烈對比色，全站色系要統一**」。
   比較 sRGB 與 OKLCH 兩種調和：OKLCH 會扭色相（芙莉蓮藍變紫、間諜家家酒綠變橄欖），**採 sRGB 混 45% `--leather #6b4a33`**，
   在 CSS 端調和（不改 76 本的 `color` 資料，日後新增的書也自動統一）。範圍含華文圈小封面與詳情頁大封面。
   ⚠️ 這一條主對話依「部分同意」逕行定案，**Frank 尚未看過調和後的全站截圖**，派工前可請 Frank 看一眼
   `.git/wp/shots/book3d-ref-home-1280.png`。

原型截圖：`.git/wp/shots/book3d-proto-*.png`（3D／牆面三案／調色兩案）。

### 6.2 契約（本輪 commit，全部先確認會紅）
- **新增** `tools/check_book3d.mjs`＋`tools/book3d_probe.html`：靜態檢查色票、`--tone`、漸層、hover／focus 轉正；
  再用 headless Chrome 以 iframe 載入五頁（首頁、有生之年、必讀經典、書庫、詳情頁）×兩個寬度（360／1280），量 computed style：
  旋轉角度、書脊、摺痕層、反光對比、地面陰影、牆面色、牆上文字對比、內容溢出、彩度、body 底色、橫向捲軸。
- **改寫** `tools/check_card_contrast.mjs`：規格變了（書本色先混皮革色），算式改為先 `--tone` 再混 `--ground`。這是主對話自己寫的契約，由主對話改，不是 builder 改測試。
- `spec.md` 新增 §11.5。
- **參考實作**（只證明契約可達成，**不給 builder**）：`.git/wp/book3d-ref-style.css`。疊上去後 `check_book3d` 110/110 全綠、其餘六組全綠。

### 6.3 為什麼這包不派免費席次
Frank 問「刻 3D 是不是不要用免費模型」，主對話同意，理由：這包的難點是**視覺判斷**，不是照規格填空——
上一輪 P-ui 免費席次測試全綠，但真 Chrome 裡淺色書卡白底白字（§2.1 教訓 1）。3D 還多了角度、窄螢幕斷字、動畫層級互蓋這些測試只能部分釘住的東西。

### 6.4 本輪踩到的坑（寫給 review 的人）
1. **首頁進場動畫會把 3D 蓋回平面**：`.shelf li > *` 的 `shelf-in` 最後一格是 `transform: none` 且 `both`，動畫層級高於一般宣告。
   參考實作的解法是改動畫用 `translate` 屬性而不是 `transform`；builder 用別的解法也可以，契約只量結果。
2. **headless Chrome 的 `--virtual-time-budget` 不推進 CSS 動畫**：probe 第一版量到 `translateX(80px)`（動畫第一格）。
   已在 probe 內 `getAnimations().finish()`。截圖則加 `--force-prefers-reduced-motion`。
3. **`pkill -f` / `pgrep -f` 會砍到自己**：指令字串本身含 `http.server 埠號` 就會被比對到，整個 shell 被殺（exit 144）。改用 `ss -ltnp` 找 pid。
4. 手機 360px 在 34 度時「SPY×FAMILY」被拆行（契約沒釘，列在工作包「必須注意」第 2 條，review 要目視）。

驗收八組：`check_book3d`、`check_card_contrast`、`check_ui`、`check_pages`、`check_library_page`、`check_home_picks`、`test_library`（node）、`test_data_status.py`。

### 6.5 本地 commit 狀態
- 本輪開始時本地領先 2（09-16 的交接 commit）、落後 6（每日排行自動 commit），已 `rebase origin/master`（只動 `data/ranking.json`，無衝突）。
- 本輪新增一個 commit：契約＋spec §11.5＋本檔。**未 push**。

### 6.6 其他待辦（沿用，未處理）
- §5 首頁第二排空白的根治（JS／CSS 加版本參數、移除安靜跳過的防禦分支）仍未做。
  3D 這包只改 CSS，新 CSS 配舊 HTML／JS 不會壞，所以兩件可分開上線；但上線後 10 分鐘內仍可能看到舊 CSS，目視驗收前先強制重整。

---

## 7. 2026-09-21 晚：P-book3d 已派工、review 完成、併回 master（未 push）

- **派工**：Sonnet agent，手動 worktree（`git worktree add` 於 706f700）。
  ⚠️ 第一次用 Agent 工具的 `isolation: worktree` 失敗：它從 **origin/master** 開，拿不到未 push 的契約，builder 正確地停下回報。**本地領先 origin 時要手動開 worktree。**
- **review 退回一次**：八組測試全綠，但截圖比對參考實作發現**書脊沒畫出來**（`::before` 轉 90 度後朝外的是背面，被 `backface-visibility: hidden` 藏掉）。
  契約只量 computed transform，量不到可見性 → 再次印證 §2.1 教訓 1。修正後目視確認書脊外露。
- **結果**：`e72deac`，只動 `style.css`。角度手機 22deg／720px 以上 32deg；360px「SPY×FAMILY」不斷字。八組測試於 worktree 與併回後的 master 皆全綠。
- builder 自己多修一處：首頁 `.book-card` 進場動畫改 `backwards`，避免 `both` 蓋掉 hover 轉正。**hover 轉正尚未用真瀏覽器實測**（headless 無法 hover），上線後請目視。
- builder 自述「server 都關了」不實，留了一支 8780，review 端代關。
- 書脊露出寬度比參考實作窄（26px × sin 角度，桌機約 14px），Frank 若覺得不夠厚可再調。
- **`/security-review`**（範圍 `origin/master..master` 4 commits，Sonnet 子代理）：**無發現**。
  範圍外、既有：`app.js` `coverHTML()` 的 `style="--c:' + color` **沒有經 `esc()`**（`cardHTML()` 有）。資料由 repo 控制、非攻擊者可達，不列發現；但 `CLAUDE.md` §5「所有欄位都經過 esc()」的敘述不精確，待 Frank 決定要不要順手補。
- **可推**：`585a224`、`b919717`、`706f700`、`e72deac`＋本節交接 commit，等 Frank 授權。
- §5 首頁第二排空白根治仍待辦。
