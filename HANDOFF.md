# 交接文件：內容補完（劇情摘要與全球榜繁中化）

> 建立日期：2026-09-05
> 指派對象：**opencode / muse spark 1.3**（本專案特例，非全域預設的 codex/luna）
> 前置狀態：排行管線已完成並上線，本文件只處理「內容」，**不需要改任何程式碼**

---

## 0. 先讀這裡：本專案現在怎麼運作

首頁有兩個榜，資料都由 `tools/fetch_ranking.py` 每日自動抓取：

| 榜 | 來源 | 範圍 |
|---|---|---|
| 華文圈熱門 | 中文維基百科單日條目瀏覽量 | 只排 `data/manga.json` 書池內的書，取前 10 |
| 全球熱門 | AniList trending | 不限書池，純展示不可點 |

**🔴 三條硬規則，違反會讓成果被自動覆蓋或被打回：**

1. **絕對不要編輯 `data/ranking.json`** —— 它是腳本產物，每天早上 8 點會被 GitHub Actions 覆寫。
2. **絕對不要 `git push`** —— 依 `~/.config/opencode/AGENTS.md` 的 push 閘門，本 repo 是 PUBLIC，一律不可自行推送。做完 commit 就停，交回主對話 review。
3. **所有 `synopsis` 與 `plot` 必須自行撰寫**（`AGENTS.md` §7 版權紅線）。**不可翻譯、不可改寫、不可複製**原作的官方簡介或維基條目內文。要寫成你自己的介紹文字。

---

## 工作包 A：把書池從 10 本擴到 30～50 本

**檔案**：`data/manga.json`

### 為什麼要做
目前書池只有 10 本，每天排來排去都是同一批，不會有作品進榜掉榜，首頁的分類標籤也永遠一樣。書池夠大，排行才有意義。

### ⚠️ 選書的關鍵限制（最容易踩的坑）

**每一本新增的書，都必須在中文維基百科有獨立條目。**

華文圈榜的名次 = 該書中文維基條目的單日瀏覽量。如果維基沒有條目，瀏覽量抓不到會記為 0，這本書就永遠沉在榜底，等於白加。

所以選書標準是：**華文圈讀者認得、且中文維基查得到條目的作品**。冷門或只有日文條目的作品不要加。

### 每本書的欄位格式

```json
{
  "id": "kebab-case-english",
  "title": "繁體中文書名",
  "author": "作者",
  "cover": "assets/covers/<id>.jpg",
  "color": "#3b4a6b",
  "genre": ["戰鬥", "奇幻"],
  "synopsis": "50~150 字的自行撰寫簡介",
  "baseScore": 88,
  "chapters": [
    { "num": 1, "title": "章節標題", "plot": "200~800 字的自行撰寫劇情摘要" }
  ]
}
```

逐欄說明：

- **`id`**：全小寫英文 + 連字號，不可重複、不可與現有的撞。決定網址，訂了就不要改。
- **`title`**：繁體中文書名。**書名裡的空格會影響維基比對**，腳本會自動嘗試去空格版，但仍建議直接寫成維基條目的樣子。
- **`cover`**：照格式填，`assets/covers/` 目前是空的，圖檔缺失時網站會自動改用直排書名的色塊封面，**這是正常設計，不要去補圖**。
- **`color`**：封面色塊的底色，選飽和度夠、在深色背景上看得清楚的顏色。避免跟現有 10 本太接近。
- **`genre`**：2～3 個，**沿用現有詞彙**（冒險／劇情／喜劇／奇幻／懸疑／戀愛／戰鬥／治癒／熱血／科幻／競技／運動／超自然／諜戰／演藝圈），不要自創新詞，否則首頁篩選標籤會碎裂。
- **`synopsis`**：50～150 字，自行撰寫。
- **`baseScore`**：0～100。**只在離線 fallback 時才用得到**，正式排序不看它，填個合理值即可（知名作品 85～95，其他 70～85）。
- **`chapters`**：**每本至少 3 話**。`plot` 每話 200～800 字繁體中文。

### 撰寫要求

- 文風要跟 `data/manga.json` 現有 10 本一致（那批也是 muse spark 寫的，直接參考）
- 每章 `plot` 是**劇情介紹**，寫「發生了什麼、對故事的意義」，不是逐格轉述對白
- 分批進行，一次 5～8 本，每批做完 commit 一次，不要一口氣塞 40 本

---

## 工作包 B：全球榜的繁中譯名與中文簡介

**檔案**：`data/global_zh.json`

### 為什麼要做
全球榜直接吐 AniList 的即時排行，書名是日文、韓文、中文原名。目前多數項目在網站上顯示「中文簡介準備中」，使用者看不懂也不知道那是什麼作品。

### 格式

```json
{
  "AniList 的原文書名": {
    "titleZh": "繁體中文譯名",
    "author": "作者",
    "genre": ["奇幻", "戰鬥"],
    "synopsis": "50~150 字的自行撰寫簡介"
  }
}
```

- key **必須完全等於** AniList 回傳的原文書名（腳本依 native → romaji → english 順序查表）
- `genre` 沿用工作包 A 的繁中詞彙，不要留 AniList 的英文分類
- 已有 3 筆範例（ONE PIECE、魔都精兵のスレイブ、ギャルの自転車を直したら懐かれた），照著寫

### 這是累積式工作，不需要一次寫完

全球榜每天會換一批書，不可能事先寫完。做法是：**跑腳本 → 看它列出哪些還沒有簡介 → 補上 → 隔天再跑**。覆蓋率隨時間提高，查不到的作品網站會照常顯示原名，不會壞掉。

```bash
python3 tools/fetch_ranking.py
```

跑完最後會印出類似這樣的待補清單：

```
⚠ 尚無中文簡介（7 筆，請補進 data/global_zh.json）：
    플레이어가 과거를 숨김
    아카데미에서 살아남기
    ...
```

**韓文書名要翻成繁體中文**，翻不出來的可用羅馬拼音查作品後再定譯名。譯名以台灣代理版的官方譯名優先；沒有正式代理的，取常見的通用譯法。

---

## 驗收（做完必跑）

```bash
cd /home/lintzuyang/freebuff_project/manga

# 1. JSON 格式合法
python3 -c "import json;[json.load(open(f)) for f in ['data/manga.json','data/global_zh.json']];print('JSON OK')"

# 2. 抓取腳本跑得完，且不能出現「維基查無條目」警告
python3 tools/fetch_ranking.py

# 3. 開站目視
python3 -m http.server 8000
```

目視檢查項目：
1. 首頁書列與華文圈榜顯示新的書，順序符合瀏覽量高低
2. 點任一本新書進 `manga.html?id=xxx`，章節列表正常
3. 點任一章節，劇情文字正常顯示、結尾有免責聲明
4. 全球榜新補的作品顯示繁中書名與中文簡介，不再是「中文簡介準備中」
5. 手機寬度 360px 無橫向捲軸

**第 2 項最重要**：出現「⚠ 維基查無條目」代表那本書的書名對不上中文維基，那本書會永遠排在榜底。要嘛改書名寫法，要嘛在 `manga.json` 該本補一個 `wiki` 欄位指定正確的條目名，要嘛換一本書。

---

## Frank 的執行指令參考

```bash
cd /home/lintzuyang/freebuff_project/manga

opencode run -m opencode/muse-spark-1.3-contributor-free \
  "讀 HANDOFF.md，執行工作包 A 的第一批：新增 6 本書到 data/manga.json。
   嚴格遵守 AGENTS.md 的資料規範與版權紅線。做完 commit，不要 push。"
```

> ⚠️ `-m` 旗標**必須明寫**。opencode 的預設模型是 `openrouter/stealth/ox-alpha`，漏了旗標它會安靜地跑成別的模型、不會報錯。

工作包 B 同理，把訊息換成「執行工作包 B」即可。

---

## 完成後

commit 完停下來，交回主對話 review diff（重點查：版權紅線有沒有踩、維基條目對不對得上、JSON 有沒有壞），review 過再由 Frank 授權 push。
